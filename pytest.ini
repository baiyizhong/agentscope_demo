[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 详细程度: -v 表示详细输出
addopts = -v

# 显示覆盖率报告
# addopts = -v --cov=config --cov=main --cov-report=html --cov-report=term-missing
